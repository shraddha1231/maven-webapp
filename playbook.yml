---
- name: Deploy to Tomcat
  hosts: web
  become: yes

  tasks:

    - name: Copy WAR file to Tomcat
      copy:
        src: target/maven-webapp.war
        dest: /opt/tomcat/webapps/maven-webapp.war

    - name: Restart Tomcat
      systemd:
        name: tomcat
        state: restarted
